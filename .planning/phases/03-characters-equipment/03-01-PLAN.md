---
phase: 03-characters-equipment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .vitepress/theme/components/CharacterTemplate.vue
  - .vitepress/theme/index.ts
  - guide/characters/index.md
  - guide/characters/kliff.md
  - guide/characters/damiane.md
  - guide/characters/oongka.md
autonomous: true
requirements: [CHAR-01]
must_haves:
  truths:
    - "CharacterTemplate.vue renders character name, class badge, image/placeholder, info grid, skills table, build cards with equipment links, and markdown slot"
    - "Three character pages (Kliff, Damiane, Oongka) exist with real game data from 03-RESEARCH.md"
    - "Character index page lists all three characters with links"
    - "CharacterTemplate is globally registered and usable via <CharacterTemplate /> in Markdown"
  artifacts:
    - path: ".vitepress/theme/components/CharacterTemplate.vue"
      provides: "Character page template with all 6 sections per UI-SPEC"
      min_lines: 80
    - path: ".vitepress/theme/index.ts"
      provides: "CharacterTemplate global registration"
      contains: "app.component('CharacterTemplate', CharacterTemplate)"
    - path: "guide/characters/kliff.md"
      provides: "Kliff character page with real skills and builds"
      contains: "<CharacterTemplate />"
    - path: "guide/characters/damiane.md"
      provides: "Damiane character page with real skills and builds"
      contains: "<CharacterTemplate />"
    - path: "guide/characters/oongka.md"
      provides: "Oongka character page with real skills and builds"
      contains: "<CharacterTemplate />"
    - path: "guide/characters/index.md"
      provides: "Character index with table linking to all three characters"
      contains: "角色職業"
  key_links:
    - from: "guide/characters/kliff.md"
      to: "CharacterTemplate.vue"
      via: "<CharacterTemplate /> tag"
      pattern: "<CharacterTemplate"
    - from: "CharacterTemplate.vue builds"
      to: "/guide/equipment/*"
      via: "withBase(equipmentLink)"
      pattern: "withBase.*equipmentLink"
---

<objective>
Create the CharacterTemplate Vue component and populate three character pages (Kliff, Damiane, Oongka) with real game data.

Purpose: Delivers CHAR-01 — players can view each character's introduction, skills, and build recommendations.
Output: CharacterTemplate.vue component, 3 character Markdown pages, updated character index page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-characters-equipment/03-CONTEXT.md
@.planning/phases/03-characters-equipment/03-RESEARCH.md
@.planning/phases/03-characters-equipment/03-UI-SPEC.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From .vitepress/theme/index.ts:
```typescript
import type { Theme } from 'vitepress'
import DefaultTheme from 'vitepress/theme'
// ... imports ...
export default {
  extends: DefaultTheme,
  enhanceApp({ app }) {
    app.component('BossTemplate', BossTemplate)
    // ... other registrations ...
  }
} satisfies Theme
```

From .vitepress/theme/components/BossTemplate.vue (pattern to follow):
```vue
<script setup lang="ts">
import { useData, withBase } from 'vitepress'
const { frontmatter } = useData()
</script>
```

From custom.css (reusable classes):
```css
.template-section { margin-bottom: 32px; }
.template-card { padding: 16px; background: var(--vp-c-bg-soft); border: 1px solid var(--vp-c-divider); border-radius: 8px; }
.template-card + .template-card { margin-top: 8px; }
```

From BossTemplate.vue (info-grid pattern to replicate):
```vue
<div class="boss-info-grid">
  <div class="template-card">
    <div class="info-label">等級</div>
    <div class="info-value">{{ frontmatter.level ?? '未知' }}</div>
  </div>
</div>
```
```css
.boss-info-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
@media (max-width: 768px) { .boss-info-grid { grid-template-columns: 1fr; } }
.info-label { font-size: 12px; font-weight: 700; color: var(--vp-c-text-3); text-transform: uppercase; margin-bottom: 4px; }
.info-value { font-size: 16px; color: var(--vp-c-text-1); }
```

From EquipmentTemplate.vue (build card pattern to replicate):
```vue
<div class="equipment-builds">
  <div v-for="build in frontmatter.builds" :key="build.name" class="template-card">
    <div class="build-name">{{ build.name }}</div>
    <div class="build-description">{{ build.description }}</div>
  </div>
</div>
```
```css
.equipment-builds { display: flex; flex-direction: column; gap: 8px; }
.build-name { font-size: 15px; font-weight: 700; color: var(--vp-c-text-1); margin-bottom: 4px; }
.build-description { font-size: 14px; color: var(--vp-c-text-2); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CharacterTemplate.vue and register globally</name>
  <files>.vitepress/theme/components/CharacterTemplate.vue, .vitepress/theme/index.ts</files>
  <read_first>
    - .vitepress/theme/components/BossTemplate.vue (info-grid pattern, image placeholder, header pattern)
    - .vitepress/theme/components/EquipmentTemplate.vue (build card pattern, image placeholder)
    - .vitepress/theme/index.ts (registration pattern)
    - .vitepress/theme/custom.css (reusable classes: template-section, template-card, info-label, info-value)
    - .planning/phases/03-characters-equipment/03-UI-SPEC.md (component spec — sections 1-6, frontmatter schema, responsive rules, copywriting)
  </read_first>
  <action>
Create `.vitepress/theme/components/CharacterTemplate.vue` with these exact 6 sections per UI-SPEC:

**1. Header section** (`.character-header.template-section`):
- `<h1>{{ frontmatter.name ?? '（未命名）' }}</h1>`
- Class-type badge: `<span v-if="frontmatter.classType" class="class-type-badge">{{ frontmatter.classType }}</span>`
- Header uses flexbox row, space-between, align-items center, flex-wrap wrap, gap 8px (same as `.boss-header`)

**2. Image section** (`.character-image.template-section`):
- `<img v-if="frontmatter.image" :src="frontmatter.image" :alt="frontmatter.name ?? ''" />` with max-width 100%, border-radius 8px
- Fallback placeholder: `<div v-else class="character-image-placeholder"><span>圖片尚未提供</span></div>` — height 200px, background `var(--vp-c-bg-soft)`, border 1px dashed `var(--vp-c-divider)`, border-radius 8px, color `var(--vp-c-text-3)`, font-size 14px, centered text

**3. Info grid section** (`.template-section`):
- `<h2>基本資訊</h2>`
- `.character-info-grid` with 3 columns (same CSS as `.boss-info-grid`): grid-template-columns repeat(3, 1fr), gap 8px
- Three `.template-card` cells, each with `.info-label` + `.info-value`:
  - 職業類型: `frontmatter.classType ?? '未知'`
  - 主武器類型: `frontmatter.mainWeapon ?? '未知'`
  - 推薦等級: `frontmatter.recommendedLevel ?? '未知'`
- Mobile responsive: `@media (max-width: 768px) { .character-info-grid { grid-template-columns: 1fr; } }`

**4. Skills table section** (`.template-section`):
- `<h2>技能列表</h2>`
- Render only if `frontmatter.skills?.length`
- Empty state if no skills: `<p v-else style="color: var(--vp-c-text-3)">技能資料尚未收集，請待後續更新。</p>`
- Standard `<table>` with `<thead>` and `<tbody>`:
  - Columns: 技能名稱 | 等級 | 效果 | 冷卻時間 | 備註
  - `<tr v-for="skill in frontmatter.skills" :key="skill.name">`
  - Cooldown: `skill.cooldown ?? '—'`
  - Note: `skill.note ?? ''`

**5. Build recommendations section** (`.template-section`):
- `<h2>建構推薦</h2>`
- Render only if `frontmatter.builds?.length`
- Empty state: `<p v-else style="color: var(--vp-c-text-3)">建構推薦資料尚待整理，歡迎參考社群討論。</p>`
- `.character-builds` container (flex column, gap 8px — same as `.equipment-builds`)
- Each build in `.template-card`:
  - `.build-name` (14px 700): `build.name`
  - `.build-description` (14px 400): `build.description`
  - Equipment links section: `<div v-if="build.equipmentLinks?.length" class="build-equipment">推薦裝備：<template v-for="(eq, idx) in build.equipmentLinks" :key="eq.text"><span v-if="idx > 0">、</span><a :href="withBase(eq.link)">{{ eq.text }}</a></template></div>`

**6. Slot section** (`.template-section`):
- `<slot />` for Markdown body content

**Script section:**
```vue
<script setup lang="ts">
import { useData, withBase } from 'vitepress'
const { frontmatter } = useData()
</script>
```

**Scoped styles** — use CSS variables only, no hardcoded colors:
- `.class-type-badge`: `display: inline-block; padding: 4px 8px; border-radius: 4px; background: var(--vp-c-brand-soft); color: var(--vp-c-brand-1); font-size: 14px; font-weight: 700;`
- `.character-info-grid`: same as boss-info-grid (grid 3-col, responsive 1-col at 768px)
- `.info-label` and `.info-value`: replicate from BossTemplate (12px/700/text-3 and 16px/text-1)
- `.character-builds`: flex column gap 8px
- `.build-name`: `font-size: 14px; font-weight: 700; color: var(--vp-c-text-1); margin-bottom: 4px;`
- `.build-description`: `font-size: 14px; color: var(--vp-c-text-2);`
- `.build-equipment`: `margin-top: 8px; font-size: 14px; color: var(--vp-c-text-2);`
- `.build-equipment a`: `color: var(--vp-c-brand-1);`

**Register in index.ts:**
Add `import CharacterTemplate from './components/CharacterTemplate.vue'` and `app.component('CharacterTemplate', CharacterTemplate)` in the enhanceApp function, following the existing pattern.
  </action>
  <verify>
    <automated>cd /Users/kengp3/Workspaces/claude_workspace/my-game && npm run docs:build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - .vitepress/theme/components/CharacterTemplate.vue exists and contains `class="character-template"`
    - CharacterTemplate.vue contains `useData` and `withBase` imports from 'vitepress'
    - CharacterTemplate.vue contains `class="class-type-badge"` with scoped CSS using `var(--vp-c-brand-soft)` background
    - CharacterTemplate.vue contains `class="character-info-grid"` with 3-column grid
    - CharacterTemplate.vue contains `<table>` with thead columns: 技能名稱, 等級, 效果, 冷卻時間, 備註
    - CharacterTemplate.vue contains `class="build-name"` with font-size 14px (NOT 15px)
    - CharacterTemplate.vue contains `withBase(eq.link)` for equipment cross-links
    - CharacterTemplate.vue contains `圖片尚未提供` placeholder text
    - CharacterTemplate.vue contains `技能資料尚未收集` empty state text
    - CharacterTemplate.vue contains `@media (max-width: 768px)` responsive breakpoint
    - .vitepress/theme/index.ts contains `import CharacterTemplate from './components/CharacterTemplate.vue'`
    - .vitepress/theme/index.ts contains `app.component('CharacterTemplate', CharacterTemplate)`
    - `npm run docs:build` exits 0
  </acceptance_criteria>
  <done>CharacterTemplate.vue created with all 6 sections matching UI-SPEC, registered globally, VitePress build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create 3 character pages and rewrite character index with real game data</name>
  <files>guide/characters/kliff.md, guide/characters/damiane.md, guide/characters/oongka.md, guide/characters/index.md</files>
  <read_first>
    - .vitepress/theme/components/CharacterTemplate.vue (the template just created — verify frontmatter schema)
    - .planning/phases/03-characters-equipment/03-RESEARCH.md (real game data for all 3 characters: skills, builds, weapons)
    - .planning/phases/03-characters-equipment/03-UI-SPEC.md (frontmatter schema, copywriting contract, index page layout)
    - guide/characters/index.md (current placeholder to replace)
    - guide/equipment/example-equipment.md (reference for frontmatter pattern)
  </read_first>
  <action>
**Create `guide/characters/kliff.md`** with this frontmatter (per D-01, D-07):
```yaml
---
layout: doc
name: 麥克道夫（Kliff Macduff）
classType: 萬能型
mainWeapon: 劍盾 / 大劍 / 長槍 / 弓 / 雙持
recommendedLevel: 遊戲開始即可玩
image: null
skills:
  - name: Armed Combat
    level: 預設
    effect: 基礎近戰武器攻擊
    cooldown: null
    note: null
  - name: Marksmanship
    level: 預設
    effect: 遠程武器攻擊，傷害與技能等級連動
    cooldown: null
    note: null
  - name: Force Palm
    level: 1
    effect: 能量攻擊，降低敵人防禦力
    cooldown: null
    note: 早期 Abyss Artifact 習得
  - name: Focus
    level: 預設
    effect: 時間減速，恢復 Spirit
    cooldown: null
    note: 特殊技能
  - name: Pump Kick
    level: 1
    effect: 雙腳飛踢
    cooldown: null
    note: 觀察學習（序幕 Boss 馬提亞斯）
  - name: Evasive Roll
    level: 被動
    effect: 閃避翻滾
    cooldown: null
    note: 觀察學習（裂角凱洛克）
builds:
  - name: 劍盾建構
    description: 搭配盾牌使用，均衡攻守。新手推薦配置，格擋消耗體力較低。
    equipmentLinks:
      - text: 主之劍
        link: /guide/equipment/example-equipment
  - name: 雙持建構
    description: 全攻擊型，最大連擊輸出。搭配主之劍與陶里亞彎刀進行高速雙持攻擊。
    equipmentLinks:
      - text: 主之劍
        link: /guide/equipment/example-equipment
  - name: 長劍建構
    description: 技能連鎖型配置，Spinning Slash、Stab、Lariat、Forward Slash 連續施放。
    equipmentLinks: []
  - name: 長槍建構
    description: 反擊型，Counter Stance 特效提供防守反擊機會。
    equipmentLinks: []
  - name: 徒手建構
    description: Iron Fist 連擊搭配摔角技術，娛樂性高的格鬥配置。
    equipmentLinks: []
  - name: 無限箭矢建構
    description: Boss 戰特化，遠程持續輸出，安全距離作戰。
    equipmentLinks: []
---
```
Markdown body below `<CharacterTemplate />`:
- h2 "角色介紹" section with a paragraph about Kliff being the main protagonist, versatile fighter, available from game start
- h2 "遊玩建議" section with tips on early game weapon choices and skill learning via Watch and Learn
- Use `::: info 資料待補充` for any sections lacking specific data (e.g., complete skill tree details)

**Create `guide/characters/damiane.md`** with frontmatter from RESEARCH:
- name: 黛米安（Damiane）
- classType: 攻擊型
- mainWeapon: 長劍 / 手槍 / 大劍 / 長槍
- recommendedLevel: 第三章解鎖
- image: null
- skills from RESEARCH: Smiting Strike, Piercing Light, Shield Toss, Holy Explosion (4 skills)
- builds from RESEARCH: 火焰槍手建構, 長劍盾牌建構, 雙持建構, 大劍建構, 長槍建構 (5 builds, all with empty equipmentLinks for now)
- Markdown body: character intro (unlocked Chapter 3, high-speed combat specialist), playstyle notes
- `::: info 資料待補充` for incomplete skill data

**Create `guide/characters/oongka.md`** with frontmatter from RESEARCH:
- name: 烏卡（Oongka）
- classType: 力量型
- mainWeapon: 雙持巨斧 / 大錘
- recommendedLevel: 第七章解鎖
- image: null
- skills from RESEARCH: Scatter Shot, Leaping Smash, Slash, Rage (4 skills)
- builds from RESEARCH: 雙持建構 (1 build with empty equipmentLinks)
- Markdown body: character intro (unlocked after Chapter 7, AOE powerhouse), Rage mechanic explanation
- `::: info 資料待補充` for most sections (less data available per RESEARCH note)

**Rewrite `guide/characters/index.md`** per UI-SPEC copywriting contract:
```markdown
---
layout: doc
title: 角色職業
---

# 角色職業

赤血沙漠各職業介紹、技能樹與建構推薦。選擇適合自己風格的職業，規劃最強配置。

## 職業列表

| 角色名稱 | 主武器類型 | 定位 | 頁面連結 |
|---------|----------|------|---------|
| 麥克道夫（Kliff Macduff） | 劍盾 / 大劍 / 長槍 / 弓 / 雙持 | 萬能型 | [查看詳情](/guide/characters/kliff) |
| 黛米安（Damiane） | 長劍 / 手槍 / 大劍 / 長槍 | 攻擊型 | [查看詳情](/guide/characters/damiane) |
| 烏卡（Oongka） | 雙持巨斧 / 大錘 | 力量型 | [查看詳情](/guide/characters/oongka) |

:::info 遊戲說明
赤血沙漠沒有傳統「職業」系統，而是三個固定可玩角色，各有獨立技能樹。技能透過深淵神器（Abyss Artifacts）或「觀察學習（Watch and Learn）」在戰鬥中習得。
:::
```
  </action>
  <verify>
    <automated>cd /Users/kengp3/Workspaces/claude_workspace/my-game && npm run docs:build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - guide/characters/kliff.md exists and contains `<CharacterTemplate />`
    - guide/characters/kliff.md frontmatter contains `name: 麥克道夫（Kliff Macduff）`
    - guide/characters/kliff.md frontmatter contains `classType: 萬能型`
    - guide/characters/kliff.md frontmatter contains at least 6 skills entries
    - guide/characters/kliff.md frontmatter contains at least 6 builds entries
    - guide/characters/kliff.md contains `link: /guide/equipment/example-equipment` in equipmentLinks
    - guide/characters/damiane.md exists and contains `<CharacterTemplate />`
    - guide/characters/damiane.md frontmatter contains `name: 黛米安（Damiane）`
    - guide/characters/damiane.md frontmatter contains at least 4 skills entries
    - guide/characters/oongka.md exists and contains `<CharacterTemplate />`
    - guide/characters/oongka.md frontmatter contains `name: 烏卡（Oongka）`
    - guide/characters/oongka.md contains `資料待補充`
    - guide/characters/index.md contains `赤血沙漠各職業介紹、技能樹與建構推薦`
    - guide/characters/index.md contains table with links to kliff, damiane, oongka
    - guide/characters/index.md does NOT contain `此內容正在撰寫中`
    - `npm run docs:build` exits 0
  </acceptance_criteria>
  <done>Three character pages with real game data render correctly via CharacterTemplate, index page lists all characters with links</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content only | No user input, no API calls — read-only wiki served as static HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | CharacterTemplate.vue frontmatter rendering | accept | All content is author-controlled Markdown frontmatter; no user-submitted data. Vue template auto-escapes interpolation (`{{ }}`). |
| T-03-02 | S (Spoofing) | withBase() equipment cross-links | accept | Links are internal to same-origin static site; no external redirect risk. |
</threat_model>

<verification>
- `npm run docs:build` passes with zero errors
- CharacterTemplate renders all 6 sections (header, image, info grid, skills table, builds, slot)
- All three character pages use real game data from 03-RESEARCH.md
- Character index links resolve to valid pages
</verification>

<success_criteria>
- CharacterTemplate.vue follows UI-SPEC exactly: 4-tier typography, CSS variables only, responsive breakpoints
- Three character pages have real game data (skills, builds, descriptions)
- Character index lists all three characters with working links
- VitePress build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-characters-equipment/03-01-SUMMARY.md`
</output>
