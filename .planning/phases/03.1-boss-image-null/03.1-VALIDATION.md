---
phase: 03.1
slug: boss-image-null
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-11
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node scripts + shell assertions (no unit test framework in repo) |
| **Config file** | none — Wave 0 creates `scripts/verify-images.mjs` |
| **Quick run command** | `node scripts/verify-images.mjs` |
| **Full suite command** | `npm run docs:build && node scripts/verify-images.mjs` |
| **Estimated runtime** | ~30 seconds (build) + ~2 seconds (verify) |

---

## Sampling Rate

- **After every task commit:** Run `node scripts/verify-images.mjs` (quick: file existence + frontmatter scan + grep checks)
- **After every plan wave:** Run `npm run docs:build && node scripts/verify-images.mjs` (full: build + dist scan)
- **Before `/gsd-verify-work`:** Full suite must be green + `npm run docs:preview` smoke check
- **Max feedback latency:** 45 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 0 | D-07 | — | N/A | infra | `test -f scripts/verify-images.mjs` | ❌ W0 | ⬜ pending |
| 03.1-01-02 | 01 | 0 | D-04 | — | N/A | infra | `test -d public/images/characters && test -d public/images/equipment && test -d public/images/bosses` | ❌ W0 | ⬜ pending |
| 03.1-02-01 | 02 | 1 | D-01..D-08 | — | N/A | integration | `node scripts/verify-images.mjs --files` (all 8 .webp exist, correct size) | ❌ W0 | ⬜ pending |
| 03.1-03-01 | 03 | 2 | D-08 | — | N/A | grep | `node scripts/verify-images.mjs --frontmatter` (all 8 .md have non-null `image:`) | ❌ W0 | ⬜ pending |
| 03.1-04-01 | 04 | 2 | withBase bug | — | Correct path resolution under base config | grep | `grep -c 'withBase(frontmatter.image)' .vitepress/theme/components/*.vue` (expect 3) | ✅ | ⬜ pending |
| 03.1-05-01 | 05 | 3 | D-all | — | N/A | build | `npm run docs:build && node scripts/verify-images.mjs --dist` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/verify-images.mjs` — node script with flags `--files`, `--frontmatter`, `--dist` that:
  - `--files`: checks all 8 `public/images/**/*.webp` exist, each < 200KB, each is valid WebP (magic bytes `RIFF....WEBP`)
  - `--frontmatter`: grep-asserts each of the 8 target .md files has `image: /images/` (not null, not empty)
  - `--dist`: after `docs:build`, checks `.vitepress/dist/my-game/images/**/*.webp` count === 8 and at least one HTML file contains `/my-game/images/`
- [ ] `public/images/characters/`, `public/images/equipment/`, `public/images/bosses/` — directories must exist
- [ ] No unit-test framework install needed — shell + node is sufficient for this phase

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual render (character page) | D-01, D-04 | Image visual quality cannot be asserted by grep | `npm run docs:preview`, open `/my-game/guide/characters/kliff`, confirm image renders (not placeholder) |
| Visual render (equipment page) | D-01, D-04 | Same as above | Open `/my-game/guide/equipment/sword-of-the-wolf`, confirm image |
| Visual render (boss page) | D-01, D-04 | Same as above | Open `/my-game/guide/bosses/example-boss`, confirm image |
| Attribution (alt text / commit msg) | D-03 | Semantic correctness of source attribution | Review each image commit message or alt text references Fextralife source URL |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 45s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
