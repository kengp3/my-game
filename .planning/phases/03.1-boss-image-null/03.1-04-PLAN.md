---
phase: 03.1-boss-image-null
plan: 04
type: execute
wave: 2
depends_on:
  - "03.1-01"
files_modified:
  - .vitepress/theme/components/CharacterTemplate.vue
  - .vitepress/theme/components/BossTemplate.vue
  - .vitepress/theme/components/EquipmentTemplate.vue
autonomous: true
requirements:
  - withBase-bug-fix
must_haves:
  truths:
    - "CharacterTemplate.vue uses :src=\"withBase(frontmatter.image)\" on its <img> tag"
    - "BossTemplate.vue uses :src=\"withBase(frontmatter.image)\" on its <img> tag"
    - "EquipmentTemplate.vue uses :src=\"withBase(frontmatter.image)\" on its <img> tag"
    - "Zero templates contain the bare :src=\"frontmatter.image\" pattern"
    - "All three templates still guard <img> with v-if=\"frontmatter.image\" (prevents withBase(null) crash)"
  artifacts:
    - path: ".vitepress/theme/components/CharacterTemplate.vue"
      provides: "Character template with correct withBase() src binding"
      contains: ":src=\"withBase(frontmatter.image)\""
    - path: ".vitepress/theme/components/BossTemplate.vue"
      provides: "Boss template with correct withBase() src binding"
      contains: ":src=\"withBase(frontmatter.image)\""
    - path: ".vitepress/theme/components/EquipmentTemplate.vue"
      provides: "Equipment template with correct withBase() src binding"
      contains: ":src=\"withBase(frontmatter.image)\""
  key_links:
    - from: "Vue templates :src binding"
      to: "VitePress withBase() runtime"
      via: "import { withBase } from 'vitepress' (already present in all 3 files)"
      pattern: "withBase\\(frontmatter\\.image\\)"
---

<objective>
Fix the `withBase()` bug in all 3 Vue SFC templates. Each template has `:src="frontmatter.image"` but MUST use `:src="withBase(frontmatter.image)"` for the production GitHub Pages deploy (`base: '/my-game/'`) to resolve image URLs correctly.

Purpose: Without this fix, all images 404 in production even though they render fine in `docs:dev` (dev server does not apply base). Satisfies Dimension 4 (Template Path Correctness) of the Validation Architecture.

Output:
- 3 edited `.vue` files, each with one single-line modification
- `node scripts/verify-images.mjs --templates` exits 0
- `withBase` imports untouched (already present in all 3 files per RESEARCH.md code review)
- `v-if="frontmatter.image"` guards preserved (prevents `withBase(null)` crash on pages without images)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-boss-image-null/03.1-RESEARCH.md
@.planning/phases/03.1-boss-image-null/03.1-UI-SPEC.md
@.planning/phases/03.1-boss-image-null/03.1-VALIDATION.md
@.planning/phases/03.1-boss-image-null/03.1-01-SUMMARY.md
@scripts/verify-images.mjs

<edit_mapping>
<!-- Exact line-level edits per RESEARCH.md "關鍵 Bug：withBase() 缺失" and UI-SPEC.md "Template Fix Contract" -->

.vitepress/theme/components/CharacterTemplate.vue  L13:  :src="frontmatter.image"  to  :src="withBase(frontmatter.image)"
.vitepress/theme/components/BossTemplate.vue       L17:  :src="frontmatter.image"  to  :src="withBase(frontmatter.image)"
.vitepress/theme/components/EquipmentTemplate.vue  L17:  :src="frontmatter.image"  to  :src="withBase(frontmatter.image)"

NOTE: withBase is already imported in all 3 files (verified in RESEARCH.md line 120: "import { useData, withBase } from 'vitepress'"). No import changes needed.

NOTE: The v-if="frontmatter.image" guard is present on the <img> element above each :src line. DO NOT REMOVE OR MODIFY IT. Without this guard, pages without images would call withBase(null) which returns "/my-game/null" — a broken link.
</edit_mapping>

<regression_guard>
<!-- Other :src= bindings in these files that must NOT be altered -->
Plan 04 only fixes the frontmatter.image binding. If any of these templates contains other :src= bindings (e.g., for icons, thumbnails), they must remain untouched. Before editing, run:

  grep -n ':src=' .vitepress/theme/components/CharacterTemplate.vue .vitepress/theme/components/BossTemplate.vue .vitepress/theme/components/EquipmentTemplate.vue

Expected output per file (before fix): exactly one `:src="frontmatter.image"` line. After fix: exactly one `:src="withBase(frontmatter.image)"` line. No additions, no removals.
</regression_guard>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix :src binding in CharacterTemplate.vue and BossTemplate.vue</name>
  <files>.vitepress/theme/components/CharacterTemplate.vue, .vitepress/theme/components/BossTemplate.vue</files>
  <read_first>
    - .vitepress/theme/components/CharacterTemplate.vue (confirm L13 has `:src="frontmatter.image"` and L12 has `v-if="frontmatter.image"`; confirm `withBase` is in the import statement at top)
    - .vitepress/theme/components/BossTemplate.vue (confirm L17 has `:src="frontmatter.image"` and L16 has `v-if="frontmatter.image"`; confirm `withBase` is imported)
    - .planning/phases/03.1-boss-image-null/03.1-RESEARCH.md (關鍵 Bug section — exact fix)
    - .planning/phases/03.1-boss-image-null/03.1-UI-SPEC.md (Template Fix Contract table)
  </read_first>
  <action>
For each of the 2 files, perform exactly one string replacement via the Edit tool:

1. `.vitepress/theme/components/CharacterTemplate.vue`
   - old_string: `        :src="frontmatter.image"`
   - new_string: `        :src="withBase(frontmatter.image)"`

2. `.vitepress/theme/components/BossTemplate.vue`
   - old_string: `        :src="frontmatter.image"`
   - new_string: `        :src="withBase(frontmatter.image)"`

(The leading 8 spaces match the indentation inside the `<img>` element as verified in the RESEARCH.md code snippets and the file reads from plan 01's context.)

If the Edit tool reports the old_string doesn't match uniquely, re-Read the file first to confirm the exact whitespace, then retry. The `:src="frontmatter.image"` occurrence should appear exactly once per file (inside the `v-if="frontmatter.image"` guarded `<img>` block).

Do NOT:
- Remove or modify the `v-if="frontmatter.image"` guard
- Add or remove imports (withBase is already imported)
- Modify any other attribute on the `<img>` element (alt, class, etc.)
- Touch the placeholder `<div v-else>` block below each `<img>`
  </action>
  <verify>
    <automated>grep -q ':src="withBase(frontmatter.image)"' .vitepress/theme/components/CharacterTemplate.vue &amp;&amp; grep -q ':src="withBase(frontmatter.image)"' .vitepress/theme/components/BossTemplate.vue &amp;&amp; ! grep -q ':src="frontmatter.image"' .vitepress/theme/components/CharacterTemplate.vue &amp;&amp; ! grep -q ':src="frontmatter.image"' .vitepress/theme/components/BossTemplate.vue &amp;&amp; grep -q 'v-if="frontmatter.image"' .vitepress/theme/components/CharacterTemplate.vue &amp;&amp; grep -q 'v-if="frontmatter.image"' .vitepress/theme/components/BossTemplate.vue &amp;&amp; grep -q "withBase" .vitepress/theme/components/CharacterTemplate.vue &amp;&amp; grep -q "withBase" .vitepress/theme/components/BossTemplate.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':src="withBase(frontmatter.image)"' .vitepress/theme/components/CharacterTemplate.vue` returns 1
    - `grep -c ':src="withBase(frontmatter.image)"' .vitepress/theme/components/BossTemplate.vue` returns 1
    - `grep -c ':src="frontmatter.image"' .vitepress/theme/components/CharacterTemplate.vue` returns 0
    - `grep -c ':src="frontmatter.image"' .vitepress/theme/components/BossTemplate.vue` returns 0
    - `grep -c 'v-if="frontmatter.image"' .vitepress/theme/components/CharacterTemplate.vue` returns 1 (guard preserved)
    - `grep -c 'v-if="frontmatter.image"' .vitepress/theme/components/BossTemplate.vue` returns 1 (guard preserved)
    - `grep -q "import.*withBase.*from 'vitepress'" .vitepress/theme/components/CharacterTemplate.vue` succeeds (import untouched)
    - `grep -q "import.*withBase.*from 'vitepress'" .vitepress/theme/components/BossTemplate.vue` succeeds (import untouched)
    - `git diff --numstat .vitepress/theme/components/CharacterTemplate.vue` shows +1/-1 (single-line edit)
    - `git diff --numstat .vitepress/theme/components/BossTemplate.vue` shows +1/-1 (single-line edit)
  </acceptance_criteria>
  <done>
    Character and Boss templates both use withBase(frontmatter.image); guards preserved; imports untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix :src binding in EquipmentTemplate.vue and run full --templates verification</name>
  <files>.vitepress/theme/components/EquipmentTemplate.vue</files>
  <read_first>
    - .vitepress/theme/components/EquipmentTemplate.vue (confirm L17 has `:src="frontmatter.image"` and L16 has `v-if="frontmatter.image"`; confirm `withBase` is imported)
    - scripts/verify-images.mjs (confirm --templates flag logic)
  </read_first>
  <action>
Perform exactly one string replacement via the Edit tool:

`.vitepress/theme/components/EquipmentTemplate.vue`
- old_string: `        :src="frontmatter.image"`
- new_string: `        :src="withBase(frontmatter.image)"`

After editing, run the harness across all 3 templates to confirm Dimension 4 is GREEN:

```bash
node scripts/verify-images.mjs --templates
```

Expected: exits 0, reports:
- 3 `withBase in ...` OK lines
- `Template path correctness: 0 bare, 3 withBase` OK line
- `VERIFY PASSED`

If any template still has the bare binding, the harness will report `BARE :src in ...` and exit 1 — re-edit the offending file.

Also verify the `v-if="frontmatter.image"` guard is still present in all three templates (prevents `withBase(null)` crash on pages that still have null image — relevant for any future placeholder pages added after this phase):

```bash
grep -c 'v-if="frontmatter.image"' .vitepress/theme/components/CharacterTemplate.vue .vitepress/theme/components/BossTemplate.vue .vitepress/theme/components/EquipmentTemplate.vue
```

Expected: each file prints 1.
  </action>
  <verify>
    <automated>grep -q ':src="withBase(frontmatter.image)"' .vitepress/theme/components/EquipmentTemplate.vue &amp;&amp; ! grep -q ':src="frontmatter.image"' .vitepress/theme/components/EquipmentTemplate.vue &amp;&amp; grep -q 'v-if="frontmatter.image"' .vitepress/theme/components/EquipmentTemplate.vue &amp;&amp; node scripts/verify-images.mjs --templates &amp;&amp; [ "$(grep -c ':src="withBase(frontmatter.image)"' .vitepress/theme/components/CharacterTemplate.vue .vitepress/theme/components/BossTemplate.vue .vitepress/theme/components/EquipmentTemplate.vue | awk -F: '{sum+=$2} END {print sum}')" -eq 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':src="withBase(frontmatter.image)"' .vitepress/theme/components/EquipmentTemplate.vue` returns 1
    - `grep -c ':src="frontmatter.image"' .vitepress/theme/components/EquipmentTemplate.vue` returns 0
    - `grep -c 'v-if="frontmatter.image"' .vitepress/theme/components/EquipmentTemplate.vue` returns 1
    - `grep -q "import.*withBase.*from 'vitepress'" .vitepress/theme/components/EquipmentTemplate.vue` succeeds
    - `node scripts/verify-images.mjs --templates` exits 0
    - Harness output contains `Template path correctness: 0 bare, 3 withBase`
    - Total `:src="withBase(frontmatter.image)"` matches across all 3 templates equals exactly 3
    - Total `:src="frontmatter.image"` bare matches across all 3 templates equals exactly 0
    - `git diff --numstat .vitepress/theme/components/EquipmentTemplate.vue` shows +1/-1
  </acceptance_criteria>
  <done>
    All 3 templates fixed, Dimension 4 is GREEN, v-if guards preserved, imports untouched.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vue template expression → HTML output | `:src` binding becomes a rendered HTML attribute; `withBase()` transforms the path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-16 | Tampering | Vue template edit | mitigate | Exact old_string/new_string pairs; single-line changes only. git diff shows +1/-1 per file. |
| T-03.1-17 | Denial of Service | `withBase(null)` call on pages without images | mitigate | `v-if="frontmatter.image"` guard preserved in all 3 templates — acceptance criteria explicitly verify its presence. |
| T-03.1-18 | Injection | XSS via frontmatter.image containing HTML | mitigate | Vue's `:src` binding escapes attribute values automatically; `withBase()` only prepends the configured base and does not perform HTML interpolation. Values come from trusted authored Markdown, not user input. |
| T-03.1-19 | Information Disclosure | Base path leak via wrong URL | mitigate | This plan's entire purpose is to fix the URL-resolution bug. The fix prevents the 404 leak pattern. |

Security enforcement scope: ASVS L1. Static template fix; no runtime user input.
</threat_model>

<verification>
Full-wave verification after both tasks complete:

1. `node scripts/verify-images.mjs --templates` → exits 0 with `0 bare, 3 withBase`
2. `grep -rc ':src="frontmatter.image"' .vitepress/theme/components/` → all files report 0
3. `grep -rc ':src="withBase(frontmatter.image)"' .vitepress/theme/components/` → sum across CharacterTemplate, BossTemplate, EquipmentTemplate equals 3
4. `grep -rc 'v-if="frontmatter.image"' .vitepress/theme/components/` → each of the 3 files reports at least 1 (guards preserved)
5. `git diff --stat .vitepress/theme/components/` → exactly 3 files changed, +3/-3 lines total

Parallelism note: This plan is in wave 2 and depends only on plan 01 (infrastructure). It has ZERO file overlap with plan 03 (which edits guide/**/*.md). Plans 03 and 04 run in parallel.
</verification>

<success_criteria>
- All 3 Vue SFC templates use `:src="withBase(frontmatter.image)"`
- Zero templates have bare `:src="frontmatter.image"`
- `v-if="frontmatter.image"` guards preserved in all 3 templates
- `withBase` imports unchanged in all 3 files
- `scripts/verify-images.mjs --templates` exits 0
- Validation Architecture Dimension 4 is GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-boss-image-null/03.1-04-SUMMARY.md` documenting:
- Before/after diff of the 3 template :src bindings
- verify-images.mjs --templates output
- Confirmation v-if guards preserved
- Confirmation withBase imports untouched
</output>
